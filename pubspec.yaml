name: envied_runner
description: Run envied generator on a single file without build_runner.
version: 1.0.0

environment:
  sdk: ^3.11.4

dependencies:
  analyzer: ^10.0.0
  build: ^4.0.5
  dart_style: any
  envied_generator: ^1.3.4
  lints: ^6.1.0
  path: ^1.9.1
  source_gen: ^4.2.2
  source_gen_test: ^1.3.5

dev_dependencies:
  test: ^1.31.0
